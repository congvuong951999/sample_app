<li id="micropost-<%= @micropost.id %>" class="showMicropost">
  <span class = "showMicropost_user">
    <%= link_to gravatar_for(@micropost.user,size:50), @micropost.user %>
    <%= link_to @micropost.user.name, @micropost.user %>
  </span>
  <div class = "showMicropost_timestamp">
    Posted <%= time_ago_in_words(@micropost.created_at)%> ago.
  </div>
  <div class="showMicropost_content">
    <strong>Comment:</strong>
    <%= @micropost.content %>
  </div>
  <span class="showMicropost_img">
    <%= image_tag @micropost.picture.url if @micropost.picture?%>
    <%# <%= image_tag @micropost.display_image if @micropost.image.attached?%>
  </span>
  <span>
    <% if current_user?(@micropost.user) %>
      <%= button_to "Delete", @micropost,method: :delete,data: {confirm:"You sure?"}, class:"btn_Delete showMicropost_btn btn_dangerous"%>
    <%end%>
  </span>
  <%= render 'comments/form'%>
  <div class="comment-group">
    <%= render @micropost.comments%>
  </div>
</li>
